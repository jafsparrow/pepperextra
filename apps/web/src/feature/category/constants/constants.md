# Category Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the category feature.

- `CATEGORY_QUERY_KEYS` — query key factory for `orpc.category.*` calls, scoped by org.
