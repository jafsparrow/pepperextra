# Category Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the category (product group) feature.

- `CATEGORY_QUERY_KEYS` — query key factory for `orpc.productGroup.*` calls, scoped by org.
- `STOCK_TRACKING_LABELS` — display labels for `group` / `sku` stock tracking modes.
