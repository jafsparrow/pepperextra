# Product Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the product feature.

- `PRODUCT_QUERY_KEYS` — query key factory for `orpc.product.*` calls, scoped by org/team.
- Labels for stock tracking mode and list column headers.
