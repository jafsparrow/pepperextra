# Customer Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the customer feature.

- `CUSTOMER_QUERY_KEYS` — query key factory for `orpc.customer.*` calls, scoped by org/team.
- `CUSTOMER_TYPE_LABELS` — display labels for `retail` / `account` / `contractor` customer types.
