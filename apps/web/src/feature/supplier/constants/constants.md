# Supplier Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the supplier (vendor) feature.

- `SUPPLIER_QUERY_KEYS` — query key factory for `orpc.supplier.*` calls, scoped by org/team.
