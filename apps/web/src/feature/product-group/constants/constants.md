# Product Group Constants

Stores feature-specific constants, TanStack Query keys, and label definitions for the product group feature.

- `PRODUCT_GROUP_QUERY_KEYS` — query key factory for `orpc.productGroup.*` calls, scoped by org.
- `STOCK_TRACKING_LABELS` — display labels for `group` / `sku` stock tracking modes.
