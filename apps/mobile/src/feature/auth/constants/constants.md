# Auth constants

Feature-specific constants and query/storage keys for mobile auth.

- Auth route paths: `/login`, `/set-password`
- SecureStore prefix handled inside `@repo/auth/client` plugins via `expoClient({ storagePrefix: "mobile" })`
