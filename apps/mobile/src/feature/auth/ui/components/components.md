# Auth components

Reusable presentational pieces for the auth screens:

- `auth-screen.tsx` — full-height themed wrapper with keyboard-avoiding layout
- `auth-text-field.tsx` — labeled TextInput
- `auth-button.tsx` — primary action button with pending state
- `auth-error.tsx` — inline error text

Screens live in `src/app/(auth)/`.
