# Auth sections

Route-level sections specific to auth.

The auth screens themselves are registered by expo-router in
`src/app/(auth)/login.tsx` and `src/app/(auth)/set-password.tsx`. Move any
larger auth page sections here if they grow beyond the screen files.
